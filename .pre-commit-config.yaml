# Exclude the subteno module as well as what's inside migrations/
# Exclude the subteno module as well as what's inside migrations/
exclude: |
  (?x)(
  ^modules/customer/subteno/ |
  __manifest__.py
  )
# Only what's inside customer module is checked
files: ^modules/customer/
default_language_version:
  python: python3
  node: "16.17.0"
repos:
  - repo: local
    hooks:
      # These files are most likely copier diff rejection junks; if found,
      # review them manually, fix the problem (if needed) and remove them
      - id: forbidden-files
        name: forbidden files
        entry: found forbidden files; remove them
        language: fail
        files: "\\.rej$"
      - id: en-po-files
        name: en.po files cannot exist
        entry: found a en.po file
        language: fail
        files: '[a-zA-Z0-9_]*/i18n/en\.po$'
  - repo: https://github.com/sbidoul/whool
    rev: v0.5
    hooks:
      - id: whool-init
  - repo: https://github.com/OCA/odoo-pre-commit-hooks
    rev: v0.0.25
    hooks:
      - id: oca-checks-odoo-module
        args: ["--disable=xml-deprecated-data-node,xml-dangerous-qweb-replace-low-priority,xml-deprecated-tree-attribute,xml-view-dangerous-replace-low-priority"]
      - id: oca-checks-po
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v2.7.1
    hooks:
      - id: prettier
        name: prettier (with plugin-xml)
        additional_dependencies:
          - "prettier@2.7.1"
          - "@prettier/plugin-xml@2.2.0"
        args:
          - --plugin=@prettier/plugin-xml
          - --print-width=120
          - --config=$HOME/Projects/Tools/pre-commit-config/.prettierrc.yml
        files: \.(css|htm|html|js|json|jsx|less|md|scss|toml|ts|xml|yaml|yml)$
  - repo: https://github.com/pre-commit/mirrors-eslint
    rev: v8.24.0
    hooks:
      - id: eslint
        verbose: true
        args:
          - --color
          - --fix
          - --config=$HOME/Projects/Tools/pre-commit-config/.eslintrc.yml
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.3.0
    hooks:
      - id: trailing-whitespace
        # exclude autogenerated files
        exclude: /README\.rst$|\.pot?$
      - id: end-of-file-fixer
        # exclude autogenerated files
        exclude: /README\.rst$|\.pot?$
      - id: debug-statements
      - id: fix-encoding-pragma
        args: ["--remove"]
      - id: check-case-conflict
      - id: check-docstring-first
      - id: check-executables-have-shebangs
      - id: check-merge-conflict
        # exclude files where underlines are not distinguishable from merge conflicts
        exclude: /README\.rst$|^docs/.*\.rst$
      - id: check-symlinks
      - id: check-xml
      - id: mixed-line-ending
        args: ["--fix=lf"]
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.3
    hooks:
      - id: ruff
        args:
        - --fix
        - --exit-non-zero-on-fix
        - --config=$HOME/Projects/Tools/pre-commit-config/.ruff.toml
      - id: ruff-format
        args:
          - --config=$HOME/Projects/Tools/pre-commit-config/.ruff.toml
  - repo: https://github.com/OCA/pylint-odoo
    rev: v9.0.4
    hooks:
      - id: pylint_odoo
        name: pylint with optional checks
        args:
          - --exit-zero
          - --rcfile=$HOME/Projects/Tools/pre-commit-config/.pylintrc
        verbose: true
      - id: pylint_odoo
        args:
          - --rcfile=$HOME/Projects/Tools/pre-commit-config/.pylintrc-mandatory
